variant: fcos
version: 1.4.0
storage:
  filesystems:
    - path: /var/opt/haproxy
      device: /dev/disk/by-partlabel/haproxy
      format: xfs
      wipe_filesystem: false
      with_mount_unit: true
