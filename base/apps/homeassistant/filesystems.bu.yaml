variant: fcos
version: 1.4.0
storage:
  filesystems:
    - path: /var/lib/homeassistant
      device: /dev/disk/by-partlabel/homeassistant
      format: xfs
      wipe_filesystem: false
      with_mount_unit: true
