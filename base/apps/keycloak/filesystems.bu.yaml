variant: fcos
version: 1.4.0
storage:
  filesystems:
    - path: /var/lib/keycloak
      device: /dev/disk/by-partlabel/keycloak-data
      format: xfs
      wipe_filesystem: false
      with_mount_unit: true
